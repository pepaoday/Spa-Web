@model DoAnPhanMem.Models.DANHMUCSP

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layoutweb.cshtml";
}

<div class="container" style="padding-top: 160px">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <h2 class="card-header text-center">Edit DANHMUCSP</h2>
                <div class="card-body">
                    @using (Html.BeginForm(null, null, FormMethod.Post, new { enctype = "multipart/form-data", @class = "form-horizontal" }))
                    {
                        @Html.AntiForgeryToken()

                        //<div class="form-group row">
                            //@Html.LabelFor(model => model.IDDM, htmlAttributes: new { @class = "control-label col-md-4" })
                            //<div class="col-md-8">
                              //  @Html.EditorFor(model => model.IDDM, new { htmlAttributes = new { @class = "form-control", placeholder = "Enter IDDM" } })
                                //@Html.ValidationMessageFor(model => model.IDDM, "", new { @class = "text-danger" })
                            //</div>
                        //</div>

        <div class="form-group">
            @Html.LabelFor(model => model.TENDM, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.TENDM, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.TENDM, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group row">
            @Html.LabelFor(model => model.IMG, htmlAttributes: new { @class = "control-label col-md-4" })
            <div class="col-md-8">
                <input type="file" name="file" id="file" class="form-control-file" />
                @Html.ValidationMessageFor(model => model.IMG, "", new { @class = "text-danger" })
                @if (!string.IsNullOrEmpty(Model.IMG))
                {
                    <img src="@Url.Content("~/Images/" + Model.IMG)" alt="Current Image" style="max-width:200px;" />
                }
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
